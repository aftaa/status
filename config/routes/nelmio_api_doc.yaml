app.swagger:
    path: /api/doc.json
    methods: GET
    defaults:
        _controller: nelmio_api_doc.controller.swagger
        area: api

app.swagger_ui:
    path: /api/doc
    controller: nelmio_api_doc.controller.swagger_ui
    defaults:
        area: api
